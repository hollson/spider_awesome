# 数据采集项目说明文档

[TOC]

## 一、项目概述

### 1.1 项目定位

**Spider Awesome** 是一个基于分层架构的**通用数据采集模板项目**，适用于：

- 🌐 **网站数据采集**：商品信息、新闻资讯、社交内容
- 🔌 **API 接口对接**：第三方数据源、开放平台接口
- 📁 **文件数据处理**：CSV、JSON、XML 等格式文件
- 📊 **定时数据同步**：数据库同步、数据仓库更新

### 1.2 核心特性

| 特性                | 说明                                                  |
| ------------------- | ----------------------------------------------------- |
| 🏗️ **分层架构**     | 采集 / 处理 / 存储 / 调度四层独立，职责清晰           |
| 📡 **自动发现**     | 采集器 `source_*.py` 自动注册，无需手动配置           |
| 🗄️ **多数据库**     | 统一 `DATABASE_URL`，支持 SQLite / PostgreSQL / MySQL |
| ⏰ **独立调度**     | 每个采集器可配置独立的 cron 调度时间                  |
| 🔄 **并发控制**     | 线程池并行采集，支持失败重试                          |
| 🌍 **环境隔离**     | dev/test/prod 三套配置，零代码切换                    |
| 🎭 **反爬防护**     | 代理池轮换、User-Agent 轮换、请求间隔控制             |
| 🌐 **JS 渲染**      | 基于 Playwright 的动态页面采集，内置反检测            |
| 🛠️ **生产级工具链** | UV 依赖管理，Ruff 代码质量，Loguru 日志               |

### 1.3 技术栈

| 类别   | 技术                                     |
| ------ | ---------------------------------------- |
| 语言   | Python 3.12+                             |
| HTTP   | Requests + 自定义重试/代理/超时          |
| 解析   | BeautifulSoup4（HTML）、json（JSON）     |
| 数据库 | SQLAlchemy 2.0（ORM）                    |
| 调度   | APScheduler                              |
| 日志   | Loguru（按天轮转）                       |
| 配置   | python-dotenv + PyYAML                   |
| 校验   | Pydantic 2.0                             |
| 浏览器 | Playwright（可选，JS 渲染）              |
| 工具   | UV（依赖）、Ruff（代码）、Bandit（安全） |

---

## 二、项目结构

```bash
spider_awesome/
├── src/                           # 核心代码
│   ├── collector/                 # 【采集层】数据拉取
│   │   ├── base_collector.py      # 采集器抽象基类
│   │   ├── base_browser_collector.py  # 浏览器采集器基类（JS 渲染）
│   │   ├── registry.py            # 采集器注册表（自动发现）
│   │   ├── source_alerion.py      # 示例：POST JSON 采集
│   │   ├── source_asl.py          # 示例：HTML 分页采集
│   │   ├── source_noble.py        # 示例：GET JSON 采集
│   │   └── source_example.py      # 示例：快速入门
│   ├── processor/                 # 【处理层】清洗、校验
│   │   ├── cleaner.py             # 数据清洗（去重/空值过滤）
│   │   └── validator.py           # 数据校验（Pydantic）
│   ├── storage/                   # 【存储层】持久化
│   │   ├── base_storage.py        # 存储抽象基类
│   │   ├── store.py               # 多数据库存储实现
│   │   ├── models.py              # ORM 模型（SQLAlchemy）
│   │   └── session.py             # 数据库会话管理
│   ├── scheduler/                 # 【调度层】定时任务
│   │   ├── task_manager.py        # 任务管理器（并发控制、失败重试）
│   │   └── tasks.py               # 任务定义
│   ├── common/                    # 公共层
│   │   ├── logger.py              # 日志（Loguru，按天轮转）
│   │   ├── http_client.py         # HTTP 客户端（重试/代理/超时/UA轮换）
│   │   ├── proxy_pool.py          # 代理池管理器
│   │   ├── user_agent.py          # User-Agent 轮换管理器
│   │   ├── browser_client.py      # Playwright 浏览器客户端
│   │   └── utils.py               # 工具函数
│   ├── env_loader.py              # 环境配置加载器
│   ├── settings.py                # 配置类
│   └── main.py                    # 主入口
├── configs/                       # 配置文件
│   ├── .env                       # 公共基础配置
│   ├── .env.dev                   # 开发环境配置
│   ├── .env.prod                  # 生产环境配置
│   ├── collectors.yml             # 采集器主配置
│   ├── collectors.example.yml     # 采集器配置完整示例
│   └── collectors.local.yml       # 采集器本地覆盖（不提交 git）
├── tests/                         # 测试
├── var/                           # 运行时数据（gitignore）
│   ├── database/                  # 数据库文件
│   ├── raw/                       # 采集器原始缓存
│   ├── logs/                      # 日志文件
│   └── output/                    # 测试覆盖率报告
├── docs/                          # 文档
├── Makefile                       # 快捷命令
├── pyproject.toml                 # 项目配置
└── README.md
```

---

## 三、快速开始

### 3.1 环境准备

```bash
# 1. 克隆项目
git clone <repo_url>
cd spider_awesome

# 2. 初始化环境（自动安装 UV、创建虚拟环境、安装依赖）
make init

# 3. 验证安装
make help
```

### 3.2 配置数据库

编辑 `configs/.env`：

```bash
# SQLite（默认，无需额外配置）
DATABASE_URL=sqlite:///var/database/spider_awesome.sqlite3

# PostgreSQL
DATABASE_URL=postgresql://postgres:password@127.0.0.1:5432/spider_awesome

# MySQL
DATABASE_URL=mysql+pymysql://root:password@192.168.101.251:3306/spider_awesome
```

### 3.3 运行采集

```bash
# 执行所有采集器（串行）
make dev

# 执行单个采集器
uv run python src/main.py run --source=alerion

# 并行执行
make run

# 启动定时调度
make scheduler
```

---

## 四、核心模块详解

### 4.1 采集层 `src/collector`

#### 基类设计

```python
# base_collector.py
class BaseCollector(ABC):
    @property
    def name(self) -> str:
        """采集器名称"""
        raise NotImplementedError

    def fetch(self) -> list[dict[str, Any]]:
        """执行采集，返回数据列表"""
        raise NotImplementedError

    def read_cache(self, filename: str) -> str | None:
        """读取缓存文件"""

    def write_cache(self, filename: str, content: str) -> None:
        """写入缓存文件"""

    def fetch_with_cache(self, cache_filename: str, fetch_func) -> str:
        """带缓存的采集"""
```

#### 浏览器采集器基类

```python
# base_browser_collector.py
class BaseBrowserCollector(BaseCollector):
    """支持 JavaScript 渲染的采集器基类"""

    def fetch_page(self, url: str, **kwargs) -> str:
        """获取渲染后的页面 HTML"""

    def execute_js(self, url: str, script: str) -> Any:
        """执行 JavaScript 脚本"""

    def fetch_page_with_cache(self, cache_filename: str, url: str, **kwargs) -> str:
        """带缓存的页面获取"""
```

#### 自动发现机制

`registry.py` 自动扫描 `source_*.py` 文件，无需手动注册：

```python
# 自动发现逻辑
def _discover_collectors() -> dict[str, type[BaseCollector]]:
    collectors = {}
    for file_path in Path(__file__).parent.glob("source_*.py"):
        name = file_path.stem.replace("source_", "")
        module = importlib.import_module(f"src.collector.{file_path.stem}")
        # 找到 *Collector 类
        for attr_name in dir(module):
            attr = getattr(module, attr_name)
            if isinstance(attr, type) and issubclass(attr, BaseCollector):
                collectors[name] = attr
    return collectors
```

#### 内置采集器

| 采集器    | 类型      | 说明                 |
| --------- | --------- | -------------------- |
| `alerion` | POST JSON | 演示 POST 请求采集   |
| `asl`     | HTML 分页 | 演示 HTML 解析和分页 |
| `noble`   | GET JSON  | 演示 GET 请求采集    |
| `example` | 快速入门  | 最简示例             |

### 4.2 处理层 `src/processor`

#### 清洗器 Cleaner

```python
class Cleaner(BaseProcessor):
    def process(self, records: list[dict]) -> list[dict]:
        # 1. 去重（基于 ID）
        records = self._deduplicate(records)
        # 2. 过滤空值记录
        records = self._filter_empty(records)
        # 3. 清理字符串字段
        records = self._clean_strings(records)
        return records
```

#### 校验器 Validator

```python
class Validator(BaseProcessor):
    def process(self, records: list[dict]) -> list[dict]:
        valid_records = []
        invalid_records = []
        for record in records:
            try:
                validated = DataRecordSchema(**record)
                valid_records.append(validated.dict())
            except Exception as e:
                invalid_records.append((record.get("id"), str(e)))
        return valid_records
```

### 4.3 存储层 `src/storage`

#### 数据库会话管理

```python
class Database:
    def __init__(self, url: str = None):
        self.url = url or settings.DATABASE_URL
        self.db_type = self._detect_db_type(self.url)
        self.engine = create_engine(self.url, ...)
        self.SessionLocal = sessionmaker(bind=self.engine)

    @contextmanager
    def get_session(self) -> Generator[Session, None, None]:
        session = self.SessionLocal()
        try:
            yield session
            session.commit()
        except Exception:
            session.rollback()
            raise
        finally:
            session.close()
```

#### ORM 模型

```python
# models.py
class DataRecord(Base):
    __tablename__ = "data_records"

    id = Column(String(64), primary_key=True)
    source = Column(String(50), nullable=False)
    collector_name = Column(String(50), nullable=False)
    title = Column(String(200))
    # ... 更多字段
```

### 4.4 调度层 `src/scheduler`

#### 任务管理器

```python
class TaskManager:
    def add_cron_job(self, job_id: str, func: Callable, hour: int, minute: int):
        """添加定时任务"""

    def add_interval_job(self, job_id: str, func: Callable, minutes: int):
        """添加间隔任务"""

    def _run_task_with_retry(self, task_id: str, func: Callable):
        """带重试的任务执行"""
```

#### 任务函数

```python
def run_collector(collector_name: str) -> dict[str, Any]:
    """执行单个采集器任务"""
    # 1. 采集
    collector = get_collector(collector_name)
    records = collector.fetch()
    # 2. 清洗
    records = Cleaner().process(records)
    # 3. 校验
    records = Validator().process(records)
    # 4. 存储
    saved = MySQLStorage(auto_create=True).save(records)
    return {"total": len(records), "success": saved}
```

---

## 五、配置系统

### 5.1 环境配置

配置优先级（从高到低）：

1. 系统环境变量
2. `.env.local`（个人本地覆盖）
3. `.env.dev` / `.env.test` / `.env.prod`（环境专属）
4. `.env`（公共基础）

```bash
# configs/.env
DATABASE_URL=sqlite:///var/database/spider_awesome.sqlite3
LOG_LEVEL=INFO
LOG_FILE=var/logs/spider_awesome.log
```

### 5.2 采集器配置

配置优先级（从高到低）：

1. ENV 环境变量：`COLLECTOR_{NAME}_{FIELD}`
2. `collectors.local.yml`（个人本地覆盖）
3. `collectors.yml`（主配置）

```yaml
# configs/collectors.yml
scheduler:
  parallel: false
  default_cron: "0 0 * * *"
  max_workers: 5
  timeout: 300
  retry: 3

collectors:
  alerion:
    enabled: true
    cron: "0 */2 * * *"
    timeout: 300
    config:
      api_key: "xxx"
```

### 5.3 ENV 覆盖示例

```bash
# 启用/禁用采集器
COLLECTOR_ALERION_ENABLED=true
COLLECTOR_NOBLE_ENABLED=false

# 覆盖调度时间
COLLECTOR_ASL_CRON=0 */3 * * *

# 覆盖超时时间
COLLECTOR_ALERION_TIMEOUT=600
```

---

## 六、反爬防护配置

本项目内置完整的反爬防护机制，包括代理池、User-Agent 轮换、请求间隔控制，以及基于 Playwright 的 JavaScript 渲染采集。

### 6.1 代理池

代理池支持多个代理轮换使用，自动检测代理有效性。

**配置项（configs/.env）：**

```bash
# 是否启用代理池（默认关闭）
PROXY_POOL_ENABLED=false

# 代理列表，用 | 分隔
# 支持格式：http://host:port 或 user:pass@host:port
PROXY_POOL_LIST=http://proxy1:8080|socks5://proxy2:1080

# 轮换策略：random（随机）/ round_robin（轮询）/ least_used（最少使用）
PROXY_POOL_STRATEGY=random
```

**代码使用：**

```python
from src.common.proxy_pool import get_proxy_pool

# 获取代理池实例
pool = get_proxy_pool(
    proxies=["http://proxy1:8080", "socks5://proxy2:1080"],
    strategy="random",
)

# 获取一个可用代理
proxy = pool.get_proxy()  # 返回 {"http": "http://proxy1:8080", "https": "http://proxy1:8080"}

# 报告代理使用结果
pool.report_success(proxy)  # 成功
pool.report_failure(proxy)  # 失败（连续3次失败自动剔除）
```

### 6.2 User-Agent 轮换

内置 20+ 常见浏览器 User-Agent，支持随机轮换。

**配置项（configs/.env）：**

```bash
# 是否启用 UA 轮换（默认启用）
UA_ROTATION_ENABLED=true

# 自定义 UA 列表，用 | 分隔（为空则使用内置列表）
UA_CUSTOM_LIST=
```

**代码使用：**

```python
from src.common.user_agent import get_ua_manager

# 获取 UA 管理器
ua_manager = get_ua_manager()

# 随机获取一个 UA
user_agent = ua_manager.get_random()

# 轮询获取下一个 UA
user_agent = ua_manager.get_next()
```

### 6.3 请求间隔控制

在请求之间添加随机延迟，避免频繁访问触发反爬。

**配置项（configs/.env）：**

```bash
# 是否启用请求间隔（默认关闭）
REQUEST_DELAY_ENABLED=false

# 请求间隔范围（毫秒），每次请求随机选择一个值
REQUEST_DELAY_MIN=1000
REQUEST_DELAY_MAX=3000
```

### 6.4 浏览器渲染（Playwright）

支持 JavaScript 渲染的动态页面采集，内置反检测脚本。

**安装依赖：**

```bash
# 安装 Playwright
uv add --optional browser playwright

# 安装 Chromium 浏览器
playwright install chromium
```

**配置项（configs/.env）：**

```bash
# 是否无头模式（默认 true）
BROWSER_HEADLESS=true

# 页面加载超时（毫秒）
BROWSER_TIMEOUT=30000
```

**代码示例：**

```python
from src.collector.base_browser_collector import BaseBrowserCollector


class DynamicPageCollector(BaseBrowserCollector):
    """动态页面采集器"""

    @property
    def name(self) -> str:
        return "dynamic_page"

    def fetch(self) -> list[dict]:
        # 获取渲染后的页面
        html = self.fetch_page(
            url="https://example.com/dynamic",
            wait_selector="#data-container",
        )

        # 解析 HTML...
        return records
```

### 6.5 HttpClient 集成

`HttpClient` 已集成所有反爬防护功能，会自动根据配置应用：

```python
from src.common.http_client import HttpClient

# 自动应用配置的反爬策略
client = HttpClient()
response = client.get("https://example.com")

# 也可以单独控制
client = HttpClient(
    use_proxy_pool=True,      # 启用代理池
    use_ua_rotation=True,     # 启用 UA 轮换
    use_delay=True,           # 启用请求间隔
)
```

---

## 七、命令参考

### 7.1 Make 命令

| 命令             | 说明                                          |
| ---------------- | --------------------------------------------- |
| `make init`      | 初始化环境（安装 UV、创建虚拟环境、安装依赖） |
| `make dev`       | 开发运行（串行）                              |
| `make run`       | 生产运行（并行）                              |
| `make scheduler` | 启动定时调度                                  |
| `make list`      | 列出所有采集器                                |
| `make status`    | 查看任务状态                                  |
| `make test`      | 运行测试                                      |
| `make lint`      | 代码检查                                      |
| `make format`    | 格式化代码                                    |
| `make bandit`    | 安全扫描                                      |
| `make clean`     | 清理项目                                      |

### 7.2 Python CLI

```bash
# 执行单个采集器
uv run python src/main.py run --source=alerion

# 执行所有采集器（串行）
uv run python src/main.py run-all

# 并行执行
uv run python src/main.py run-parallel

# 指定采集器并行
uv run python src/main.py run-parallel --collectors=alerion,asl

# 试运行（不保存）
uv run python src/main.py run --source=alerion --dry-run

# 启动调度器
uv run python src/main.py scheduler

# 列出采集器
uv run python src/main.py list

# 查看状态
uv run python src/main.py status
```

---

## 八、扩展开发

### 8.1 新增采集器

**Step 1: 创建文件**

```python
# src/collector/source_mymusic.py
from src.collector.base_collector import BaseCollector
from src.common.logger import logger


class MyMusicCollector(BaseCollector):
    """音乐数据采集器"""

    API_URL = "https://api.example.com/music"

    @property
    def name(self) -> str:
        return "mymusic"

    def fetch(self) -> list[dict]:
        logger.info(f"[{self.name}] 开始采集")

        # 1. 获取数据（带缓存）
        content = self.fetch_with_cache(
            cache_filename="mymusic_raw.json",
            fetch_func=self._make_request,
        )

        # 2. 解析数据
        data = json.loads(content)
        records = [self._parse_item(item) for item in data["items"]]

        logger.info(f"[{self.name}] 采集完成，共 {len(records)} 条")
        return records

    def _make_request(self) -> str:
        response = self.http_client.get(self.API_URL)
        return response.text

    def _parse_item(self, item: dict) -> dict:
        return {
            "id": self.generate_record_id(item["id"]),
            "source": self.name,
            "collector_name": self.name,
            "title": item["name"],
            # ... 更多字段
        }
```

**Step 2: 添加配置**

```yaml
# configs/collectors.yml
collectors:
  mymusic:
    enabled: true
    cron: "0 */6 * * *"
    timeout: 300
```

**Step 3: 完成**

```bash
python src/main.py list                    # 看到 mymusic
python src/main.py run --source=mymusic    # 执行采集
```

### 8.2 新增数据模型

```python
# src/storage/models.py
class MusicRecord(Base):
    __tablename__ = "music_records"

    id = Column(String(64), primary_key=True)
    source = Column(String(50), nullable=False)
    title = Column(String(200))
    artist = Column(String(100))
    album = Column(String(100))
    duration = Column(Integer)
    # ... 更多字段
```

### 8.3 新增处理器

```python
# src/processor/enricher.py
class Enricher(BaseProcessor):
    """数据增强器"""

    def process(self, records: list[dict]) -> list[dict]:
        for record in records:
            # 添加地理信息、标签等
            record["tags"] = self._extract_tags(record)
        return records
```

---

## 九、审计日志

系统内置轻量级审计日志功能，记录每次采集周期的执行结果。

### 9.1 数据库表结构

| 字段              | 类型        | 说明                           |
| ----------------- | ----------- | ------------------------------ |
| `collector_name`  | VARCHAR(50) | 采集器名称                     |
| `status`          | VARCHAR(10) | 执行状态: success/partial/fail |
| `data_count`      | INTEGER     | 采集数据条数                   |
| `new_count`       | INTEGER     | 新增条数                       |
| `duplicate_count` | INTEGER     | 去重条数                       |
| `duration`        | FLOAT       | 执行耗时(秒)                   |
| `error_msg`       | TEXT        | 错误信息(失败时)               |
| `created_at`      | TIMESTAMP   | 执行时间                       |

### 9.2 查看审计日志

```bash
# 查看最近24小时日志
make logs

# 指定采集器
uv run python src/main.py logs --collector=alerion

# 查看最近48小时，最多50条
uv run python src/main.py logs --hours=48 --limit=50
```

### 9.3 使用场景

- **监控采集状态**：快速查看哪些采集器正常运行
- **问题排查**：失败时查看错误信息
- **性能分析**：观察采集耗时变化趋势
- **告警基础**：可扩展为连续失败告警

---

## 十、部署指南

### 10.1 本地开发

```bash
# 初始化
make init

# 开发运行
make dev

# 启动调度
make scheduler
```

### 10.2 服务器部署

```bash
# 1. 克隆代码
git clone <repo_url>
cd spider_awesome

# 2. 初始化
make init

# 3. 配置生产环境
export ENV_MODE=prod
# 编辑 configs/.env.prod

# 4. 后台运行
nohup make scheduler > /var/log/spider.log 2>&1 &
```

### 10.3 Docker 部署

```dockerfile
FROM python:3.12-slim

WORKDIR /app
COPY . .
RUN pip install uv && uv sync

CMD ["uv", "run", "python", "src/main.py", "scheduler"]
```

---

## 十一、常见问题

### Q1: 如何添加新的采集器？

A: 只需在 `src/collector/` 下创建 `source_xxx.py` 文件，继承 `BaseCollector`，实现 `fetch()` 方法即可。无需修改其他代码，采集器会自动发现。

### Q2: 如何为不同采集器设置不同的调度时间？

A: 在 `configs/collectors.yml` 中为每个采集器配置独立的 `cron` 字段：

```yaml
collectors:
  alerion:
    cron: "0 */2 * * *" # 每 2 小时
  asl:
    cron: "30 0 * * *" # 每天 00:30
```

### Q3: 如何切换数据库？

A: 修改 `configs/.env` 中的 `DATABASE_URL`：

```bash
# SQLite
DATABASE_URL=sqlite:///var/database/spider_awesome.sqlite3

# PostgreSQL
DATABASE_URL=postgresql://user:pass@host:port/dbname

# MySQL
DATABASE_URL=mysql+pymysql://user:pass@host:port/dbname
```

### Q4: 如何调试采集器？

A: 使用 `--dry-run` 参数试运行，不保存到数据库：

```bash
uv run python src/main.py run --source=alerion --dry-run
```

### Q5: 日志文件在哪里？

A: 默认在 `var/logs/spider_awesome.log`，按天轮转，保留 7 天。可在 `configs/.env` 中配置：

```bash
LOG_FILE=var/logs/spider_awesome.log
LOG_RETENTION=7
```

---

## 十二、健壮性设计

本项目针对**无人值守**场景设计了多层健壮性保障，确保单个采集器或站点故障不会阻塞整个系统。

### 12.1 错误分层处理

| 层级    | 组件            | 健壮性机制                                     |
| ------- | --------------- | ---------------------------------------------- |
| HTTP 层 | `HttpClient`    | 自动重试（3次）、超时控制（30s）、错误分类提示 |
| 采集层  | `BaseCollector` | `safe_fetch()` 异常捕获，失败返回空列表        |
| 任务层  | `tasks.py`      | 单个采集器失败 `continue`，不影响其他采集器    |
| 调度层  | `task_manager`  | 定时任务独立调度，互不干扰                     |

### 12.2 safe_fetch 机制

所有调度任务使用 `safe_fetch()` 而非直接调用 `fetch()`：

```python
# BaseCollector.safe_fetch()
def safe_fetch(self) -> list[dict]:
    try:
        return self.fetch()
    except KeyboardInterrupt:
        raise  # 允许用户中断
    except Exception as e:
        logger.error(f"[{self.name}] 采集异常，已降级: {e}")
        return []  # 返回空列表，不阻塞调度
```

**设计原则**：采集失败 = 返回空数据，不抛异常，不阻塞后续任务。

### 12.3 HTTP 错误分类

`HttpClient` 自动识别常见网络错误并给出中文提示：

| 错误类型     | 提示信息                                    |
| ------------ | ------------------------------------------- |
| SSL 证书错误 | SSL证书验证失败，请检查网络或站点证书配置   |
| 连接失败     | 连接失败，请检查网络或站点是否可访问        |
| 请求超时     | 请求超时，请稍后重试                        |
| 404          | 接口不存在（404），请确认地址是否正确       |
| 403          | 访问被拒绝（403），请检查权限或IP是否被限制 |
| 5xx          | 服务端异常，请稍后重试                      |

### 12.4 重试策略

```yaml
# configs/collectors.yml
scheduler:
  retry: 3 # 失败重试次数
  retry_delay: 60 # 重试间隔（秒）
  timeout: 300 # 单任务超时（秒）
```

HTTP 层默认重试 3 次（指数退避），仅对 5xx 错误重试，4xx 不重试。

### 12.5 无人值守保障

| 场景           | 处理方式                          |
| -------------- | --------------------------------- |
| 目标站点下线   | 返回空列表，记录日志，继续执行    |
| 网络超时       | 重试 3 次，失败后降级             |
| 数据库异常     | 事务回滚，记录失败，继续执行      |
| 采集器代码异常 | `safe_fetch` 捕获，返回空列表     |
| 单个采集器崩溃 | 不影响其他采集器，`continue` 跳过 |

### 12.6 日志层级

```
[HTTP] GET https://xxx → 404 (1.4s) - 接口不存在（404），请确认地址是否正确  ← 详细错误
[Task] 采集失败: example_noble (1.4s)                                       ← 简要提示
[Task] example_noble 执行失败                                                 ← 汇总提示
```

错误详情只记录一次，上层只记录简要信息，避免日志刷屏。

---

## 十三、相关文档

- [数据采集最佳设计方案](数据采集最佳设计方案.md) - 架构设计和设计原则
- [Python编码规范](Python编码规范.md) - 代码风格和命名规范
- [README.md](../README.md) - 项目概述和快速开始
- [collectors.example.yml](../configs/collectors.example.yml) - 采集器配置完整示例
